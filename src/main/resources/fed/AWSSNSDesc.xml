<!-- This type defines the filter configuration data for the Publish SNS Message filter
which publishes messages to AWS SNS topics from Axway API Gateway "class" - The
filter classname. "topicArn" - The ARN of the AWS SNS topic "awsRegion" - The AWS 
region where the topic is located "messageSubject" - The subject of the message
"messageStructure" - The structure of the message (default, json) "messageAttributes" - 
JSON format message attributes "maxRetries" - Maximum number of retry attempts 
"retryDelay" - Delay between retry attempts in milliseconds -->

<entityStoreData>
	<entityType name="PublishSNSMessageFilter" extends="AWSFilter">
		<constant name="class" type="string" value="com.axway.aws.sns.PublishSNSMessageFilter" />
		<field name="topicArn" type="string" cardinality="1" default="" />
		<field name="awsRegion" type="string" cardinality="1" default="us-east-1" />
		<field name="messageSubject" type="string" cardinality="1" default="" />
		<field name="messageStructure" type="string" cardinality="1" default="default" />
		<field name="messageAttributes" type="string" cardinality="1" default="" />
		<field name="maxRetries" type="string" cardinality="1" default="3" />
		<field name="retryDelay" type="string" cardinality="1" default="1000" />
		<field name="credentialType" type="string" cardinality="1" default="local" />
		<field name="useIAMRole" type="string" cardinality="1" default="false" />
		<field name="awsCredential" type="^ApiKeyProfile" cardinality="?" />
		<field name="clientConfiguration" type="^AWSClientConfiguration" cardinality="?">
			<defaultRef>
				<key type="AWSSettings">
					<id field="name" value="Amazon Web Services Settings"/>
					<key type="AWSClientConfiguration">
						<id field="name" value="Default AWS Client Configuration"/>
					</key>
				</key>
			</defaultRef>
		</field>
		<field name="credentialsFilePath" type="string" cardinality="1" default="" />
	</entityType>

	

	<!-- Internationalization for logging / audit trail -->
	<entity xmlns="http://www.vordel.com/2005/06/24/entityStore"
		type="InternationalizationFilter">
		<key type="Internationalization">
			<id field="name" value="Internationalization Default" />
		</key>
		<fval name="type">
			        <value>PublishSNSMessageFilter</value>
		</fval>
		<fval name="logFatal">
			        <value>Error in the Publish SNS Message filter. Error: ${circuit.exception}</value>
		</fval>
		<fval name="logFailure">
			        <value>Failed in the Publish SNS Message filter</value>
		</fval>
		<fval name="logSuccess">
			        <value>Success in the Publish SNS Message filter</value>
		</fval>
	</entity>

</entityStoreData> 